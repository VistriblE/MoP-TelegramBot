export class User {
    // name: string;
    // surName: string;
    // group: number;
    // course: number;
  
    constructor(private name: string, private surName: string, group: number, course: number ){}

    
  }
  